using Microsoft.AspNetCore.Identity;

namespace AutodocConnector.Persistence.Models;

/// <summary>
/// RoleClaim entity (Identity entity). Expand here if need.
/// </summary>
internal class RoleClaim : IdentityRoleClaim<Guid>
{
}
